# Monitoring services
# This is a placeholder - prometheus.yml and grafana.yml should be used instead

services:
  prometheus:
    extends:
      file: prometheus.yml
      service: prometheus

  grafana:
    extends:
      file: grafana.yml
      service: grafana
